# The Github Instructions
```
git init (do this only the first time)
git add . (do this only the first time)
git commit -am "This is my commit"
git remote add origin https://github.com/Arrowson/test.git (do this only the first time)
git push -u origin master
```

